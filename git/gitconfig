[credential]
	helper = store
[alias]
	co = checkout
	ci = commit
	br = branch
	ll = log --decorate --graph --oneline
	mf = merge --ff-only
	st = status
	conflicted = !vim +Conflicted
[core]
	editor = vim
[user]
	name = Mikhail Pokhikhilov
[cinnabar]
	version-check = 1661868318
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[difftool "diffpdf"]
  cmd = diff-pdf --view \"$LOCAL\" \"$REMOTE\"
[init]
	defaultBranch = master
